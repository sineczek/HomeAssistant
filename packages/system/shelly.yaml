##################################################################################
##################################### SHELLY #####################################
##################################################################################

shelly:
  cloud_auth_key: !secret shellyauth
  cloud_server: !secret shellycloud
  discovery: true #add all devices (default)
  sensors:
    - all